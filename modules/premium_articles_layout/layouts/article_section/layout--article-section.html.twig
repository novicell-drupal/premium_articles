{#
/**
 * @file
 * Default theme implementation to display a one-column layout.
 *
 * Available variables:
 * - content: The content for this layout.
 *   All of the following might be available if enabled in the article settings.
 *   - content.content: The main content and content of the content region.
 *   - content.title: The title of the article.
 *   - content.description: The description of the article.
 *   - content.list_date: The listed date for this articles.
 *   - content.article_type: Link to the taxonomy term that is the type of this article.
 *   - content.article_categories: Links to all the taxonomy terms that are the categories of this article.
 *   - content.user_profile: A rendered user profile of the author of this article.
 * - attributes: HTML attributes for the layout <div>.
 *
 * @ingroup themeable
 */
#}

{% set classes = [
  'layout',
  'layout--article_section',
  'section'
] %}

<section {{ attributes.addClass(classes) }}>
  <div class="container">
    {% if header and header is not empty %}
      <div class="row">
        <div class="layout-builder__section--header">
          {{ header }}
        </div>
      </div>
    {% endif %}
    {% if content.title or content.description or content.list_date or content.article_type or content.article_categories %}
      <div class="row">
        <div class="col-xs-12">
          {% if content.title %}
            <h1>{{ content.title }}</h1>
          {% endif %}
          {% if content.description %}
            <h2>{{ content.description }}</h2>
          {% endif %}
          <hr/>
          {% if content.list_date %}
            {{ content.list_date }} •
          {% endif %}
          {% if content.article_type %}
            {{ content.article_type }} •
          {% endif %}
          {% if content.article_categories %}
            {{ content.article_categories }}
          {% endif %}
        </div>
      </div>
    {% endif %}
    <div class="row">
      <div {{ region_attributes.content.addClass(['col-xs-12 col-md-8', 'layout__region', 'layout__region--first']) }}>
        {{ content.content }}
      </div>
    </div>
  </div>
</section>
